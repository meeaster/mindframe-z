# Implementation Plans

Generated by the improve skill on 2026-07-06 (audit at commit `ba63dbf`, standard effort, full-repo scope; not audited in depth: `src/tui/`, `src/skills/`, `src/ref-store/`, sandbox broker internals, `sandbox/` Dockerfiles). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Abort apply on unparseable local Claude JSON; atomic $HOME/thread-state writes | P1 | M | — | TODO |
| 002 | CI workflow enforcing `pnpm check` + integration suite | P1 | S | — | TODO |
| 003 | Narrow thread-dispatch mount to archive-cache (secrets exposure) | P1 | M | — | TODO |
| 004 | Create zsh secrets file 0600 / secrets dir 0700 | P1 | S | 001 | TODO |
| 005 | Timeout + container kill for hung thread dispatches | P2 | S | — (rebase after 003) | TODO |
| 006 | Warn on corrupt thread manifests instead of silent skip | P2 | S | — | TODO |
| 007 | Claude transcript index — one walk per command, not per session | P2 | S | — | TODO |
| 008 | Advisory lockfile serializing mutating `mfz thread` commands | P2 | M | 001 (recommended) | TODO |
| 009 | Unit tests for manifests + claude renderer merges | P2 | M | 001 | TODO |
| 010 | Bounded-concurrency S3 session backup uploads | P2 | S | — | TODO |
| 011 | Reconcile the OpenSpec change ledger with shipped code | P2 | S | — | TODO |
| 012 | Fix README `sandbox observe` / `cc` / `oc` command docs | P3 | S | — | TODO |
| 013 | Keep the Agent Vault token out of docker argv | P3 | M | — | TODO |
| 014 | Bump `@types/node` to 26.x | P3 | S | — | TODO |
| 015 | Release-history engine (phase 1 of designed release workflow) | P3 | M | — | TODO |
| 016 | Spike: `mfz thread eval` stage-only dispatch | P3 | M | — | TODO |
| 017 | Spike: lapdog cost-span envelope feasibility | P3 | S | — | TODO |
| 018 | Spike: ChatGPT session ingestion design | P3 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **004 requires 001**: it writes through `writeFileAtomic`'s `mode` parameter, which 001 creates.
- **009 requires 001**: it characterizes `readJsonObject`'s post-001 abort behavior; writing the tests first would pin the buggy behavior.
- **008 recommends 001 first**: atomic writes bound the damage of any race until the lock lands; both touch thread write paths.
- **005 and 003 both edit `src/thread/runner.ts`** (different regions): land 003 first, rebase 005 — or vice versa; no semantic dependency.
- **006, 007, 008 all touch `src/thread/sweep.ts`** in different regions — execute serially, not in parallel worktrees, to avoid merge noise.
- **002 (CI) early** multiplies the value of every later plan's tests (009, and the new tests in 001/003/005/006/007/008/010/013).
- **017 gates the lapdog build-out**: the openspec change `lapdog-thread-observability` should not be executed further until 017's findings land (its own design says spike first).
- **011 before 015/017 is tidy but not required** — 011 corrects the same ledgers those plans read/update.

## Findings considered and rejected

- **gitconfig include dedup can relocate a user's `[includeIf]`-scoped include** (`src/core/git-config.ts:56`): confirmed mechanism, but requires the user to have hand-placed the exact managed include path under a conditional section — unlikely enough that the fix isn't worth the code. Revisit only if a real report surfaces.
- **CLI command taxonomy inconsistency** (`cc`/`oc` top-level, `observe` under `thread`, `sandbox` its own group): a maintainer taste call, not a defect. Plan 012 fixes the docs to match the code; rehoming commands is left to the maintainer.
- **`docs/model-ids.md` is orphaned and will rot**: real but trivial — delete or link it when convenient; doesn't merit a plan.
- **Horizon-2 sandbox direction (profile-driven `mfz sandbox up` for any profile, MCP front door, core/personal repo split)**: explicitly parked — `docs/vision-mindframe-z.md` gates it on "CLI primitives must stabilize first" and there are no external users yet.
- **`.catch(() => {})` on `wx` placeholder writes** (`src/sandbox/runtime.ts:362,380,399`): swallows EACCES along with intended EEXIST — LOW impact (placeholder files); fold into any future sandbox runtime work rather than a standalone plan.
- **`listRunStatuses` swallows corrupt per-run `status.json`** (`src/thread/observability.ts:64-66`): disposable operational files; silent skip is acceptable there (contrast plan 006's thread manifests, which are durable state).

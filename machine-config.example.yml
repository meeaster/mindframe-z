# Machine-local AI config. Place this at ~/.mindframe-z/config.yml.
# Overrides the profile selection, references directory, and machine-specific
# OpenCode overrides for this machine.
# This file is not checked into the repo — it's per-machine config.
profile: personal
home_path: ~/code/mindframe-z
references_dir: ~/.mindframe-z/references

# git:
#   name: Your Name
#   email: you@example.com

# sandbox:
#   credentials: subscription # or bedrock

# thread:
#   stores:
#     - name: work
#       root: ~/workspace/knowledge/work-knowledge
#       path: threads
#       publication: direct
#       default: true
#     - name: personal
#       root: ~/workspace/knowledge/personal-knowledge
#       path: threads
#       publication:
#         mode: pull-request
#         base: main # publish from a disposable worktree and stop after opening a PR

# Durable work-unit content can live in a versioned Personal knowledge repository.
# Runtime bindings and delivery state remain under ~/.mindframe-z/work/v1.
# work:
#   units_root: ~/workspace/knowledge/personal-knowledge/work-units

# extra_folders:
#   - path: ~/code/work/my-work-repo
#     description: Work project with customer-specific schemas, CI config, and deployment scripts — needed when modifying deploy logic or reading env-specific overrides
#     url: https://github.example.com/org/my-work-repo  # upstream URL when the folder is a git clone — a thread digest surfaces it in Sources so a reader can reopen it
#   - path: ~/code/personal/side-project
#     description: Personal side project with experimental code patterns and reference implementations
#     edit: deny
#   - path: ~/code/archived
#     description: Archived projects for reference — source patterns and past architecture decisions
#     read: deny
#     edit: deny

# opencode:
#   permission:
#     websearch: allow
#     external_directory:
#       /home/mark/work/**: allow
#     edit:
#       /home/mark/references/**: deny
